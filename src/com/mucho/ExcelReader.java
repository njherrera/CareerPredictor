package com.mucho;

// creates player objects from data set (probably excel)
public class ExcelReader {


    /* buildCareers (dataset) method
        some kind of for each loop that goes through dataset row by row
          if season is not included in an existing player's career, either add it to an existing player's career or use it as the first season of a new player's career

        HashMap to hold player's catalogued so far?
            checking to see if a player's already been created would just be checking to see if HashMap.get(player name) returns null
     */
}
