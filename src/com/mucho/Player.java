package com.mucho;

import java.util.ArrayList;

public class Player {
    
    private ArrayList<Player> similarPlayers;

    /*checkSimilarity METHOD
            compare this player's statisical output/growth to another player

            compare prospect's first seasons to historical player (raw stats)

            if prospect's career has more than one season:
                compare growth across seasons to historical player
            create composite score from raw stats/growth
                i.e. if prospect's raw stats are 50% similar and growth 70% similar, composite is 60&%*/

/*    makeProjections METHOD
            input: given player, similarPlayers
            how to project?
            what to return/do?*/

}
